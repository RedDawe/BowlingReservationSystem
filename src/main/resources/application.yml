spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bowling_reservation_system
    username: postgres
    password: pass1234
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  error:
    include-message: always
